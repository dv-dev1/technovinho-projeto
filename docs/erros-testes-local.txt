TECHNOVINHO - Registro de erros em testes locais

Data de inicio: 2026-05-26
Responsavel: Codex + Lucio

Formato de registro:
- Data/hora:
- Area:
- Passos para reproduzir:
- Resultado esperado:
- Resultado obtido:
- Severidade:
- Status:
- Observacoes:

Registros:

1.
- Data/hora: 2026-05-26
- Area: Ambiente local / bootstrap
- Passos para reproduzir:
  Tentar iniciar a aplicacao localmente a partir do workspace.
- Resultado esperado:
  Docker e/ou Python operacional para subir backend, frontend e banco.
- Resultado obtido:
  `docker` nao encontrado no ambiente e `python` do PATH aponta para o alias da Windows Store, falhando ao executar scripts.
- Severidade:
  Alta
- Status:
  Resolvido no ambiente local
- Observacoes:
  Docker Desktop foi instalado e a stack passou a subir com `docker compose`.

2.
- Data/hora: 2026-05-26
- Area: Backend / migration inicial
- Passos para reproduzir:
  Subir a stack com `docker compose up --build -d` em banco PostgreSQL vazio.
- Resultado esperado:
  A migration `001_initial_schema` deve criar schema inicial e iniciar a API.
- Resultado obtido:
  A API encerra durante o bootstrap com erro `type "user_role" already exists`.
- Severidade:
  Alta
- Status:
  Corrigido localmente e validado
- Observacoes:
  A migration criava os enums manualmente e o SQLAlchemy tentava cria-los de novo ao criar as tabelas.

3.
- Data/hora: 2026-05-26
- Area: Backend / regra de agendamento
- Passos para reproduzir:
  Com um servico de 45 minutos e disponibilidade do profissional de 09:00 a 18:00 na segunda-feira, criar agendamento para `2026-06-01T17:50:00Z`.
- Resultado esperado:
  A API deveria rejeitar o agendamento, pois o atendimento terminaria apos o fim da faixa disponivel.
- Resultado obtido:
  A API aceitou o agendamento e retornou `201 Created`.
- Severidade:
  Alta
- Status:
  Corrigido localmente e validado
- Observacoes:
  A validacao passou a considerar a duracao do servico contra o horario final da disponibilidade e tambem conflito com agendamentos ativos do mesmo profissional.

4.
- Data/hora: 2026-05-26
- Area: Ambiente local / validacao final APS
- Passos para reproduzir:
  Executar validacao final do card de entrega com `docker compose up --build -d` e checagem dos endpoints locais.
- Resultado esperado:
  Stack sobe com backend, frontend e banco disponiveis nas portas padrao.
- Resultado obtido:
  Validado com sucesso. `docker compose ps` mostrou `api`, `frontend` e `db` em estado `Up`; `http://127.0.0.1:8000/api/health` retornou `200`; `http://127.0.0.1:8501` retornou `200`.
- Severidade:
  Informativo
- Status:
  Validado
- Observacoes:
  Persistiu apenas o bloqueio do Python local resolvido pelo alias `WindowsApps`, impedindo reexecucao de testes fora do Docker nesta sessao.

5.
- Data/hora: 2026-05-26
- Area: Ambiente local / execucao de suites
- Passos para reproduzir:
  Rodar as suites com containers temporarios montando o workspace atual do repositorio.
- Resultado esperado:
  Conseguir validar integracao e frontend mesmo com o Python local do Windows indisponivel para execucao direta.
- Resultado obtido:
  Validado com sucesso. Suite de integracao: `40 passed`. Suite frontend/UI: `37 passed` e `7 subtests passed`.
- Severidade:
  Informativo
- Status:
  Validado
- Observacoes:
  Foi identificado e corrigido um problema de isolamento da sessao de banco para ambientes de teste e uma fragilidade temporal no teste UI de confirmacao de agendamento.
